<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/BaseTheme">
        <item name="android:colorControlNormal">@color/main_theme_color</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="LoginTheme" parent="@style/BaseTheme_Login">
        <item name="android:colorControlNormal">@color/main_theme_color</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <style name="PlayTheme" parent="@style/BaseTheme_Login">
        <item name="android:colorControlNormal">@color/main_theme_color</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <style name="ProgressBarStyle" parent="@android:style/Widget.Material.ProgressBar.Small" />
</resources>
